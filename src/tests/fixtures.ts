import type { UserProfile } from "../types";

export const BASE_PROFILE: UserProfile = {
  commuteKmPerDay: 10,
  transportMode: "car",
  electricityUnitsPerMonth: 100,
  dietType: "mixed",
  onlineOrdersPerMonth: 2,
  shoppingFrequency: "medium",
  recyclesWaste: true,
  usesACDaily: false,
};
